import { test } from "node:test";
import assert from "node:assert/strict";

// Placeholder to keep `npm test` green in Phase 0; real coverage lands with
// the sharp/ffmpeg pipelines in Phase 5a.
test("placeholder", () => {
  assert.equal(1 + 1, 2);
});
